servlet默认情况下在用户第一次访问的时间创建servlet对象  执行init方法（只会执行一次）
以后再用户每次访问servlet的时候都会创建一个新的线程去执行servlet方法（每请求一次就会访问一次 执行多次）  
servlet在服务器正常关闭的时候销毁，在销毁之前会调用destroy方法（只会执行一次）
		load-on-statup 正数 就是创建时创建 负数就是用户第一次调用时创建
		servlet对象是单例对象
服务器是 多线程执行